using UnityEngine;
using System.Collections;

public class username : MonoBehaviour {
    private UILabel label;

	// Use this for initialization
	void Start () {
        label = this.GetComponent<UILabel>();
        label.text = "角色名";
        //textlist.Add(textlist);
	
	}
	
	// Update is called once per frame
	void Update () {


	
	}
}
